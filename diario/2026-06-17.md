# Diario de Bordo - 2026-06-17

## Sessoes do Dia

- **Correcao DNS Cohen (M70q):** Diagnosticado APIConnectionError no gateway Hermes do Cohen ao contactar API DeepSeek. Causa raiz: systemd-resolved travado no stub 127.0.0.53 apos reboot/update. Interface primaria do Cohen e wlo1 (WiFi), nao eno2 (Ethernet).
- **Correcao:** `sudo resolvectl dns wlo1 8.8.8.8 1.1.1.1` + `sudo netplan apply`. Persistencia via Netplan (`/etc/netplan/50-cloud-init.yaml`) ja tinha DNS configurado, mas nao propagava para o systemd-resolved.
- **Chave SSH:** Chave ed25519 antiga perdida (sem privada no `~/.ssh/` do Latitude). Gerada nova: `ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519`. Publica instalada no `authorized_keys` do Christian no Cohen.
- **Verificacao:** DNS funcional (`resolvectl query api.deepseek.com` = 3.173.21.63, 77.7ms), API DeepSeek alcancavel (HTTP 401 = endpoint vivo).

## Decisoes e Aprendizados

- Cohen usa wlo1 (WiFi) como interface primaria — diagnostico inicial errou ao focar eno2
- Netplan com `dhcp4-overrides: use-dns: false` funciona, mas as vezes nao propaga pro resolved
- Chave SSH deve ser salva em local seguro apos geracao para evitar recriacao

## Metricas

- Sessoes: 1 (correcao DNS)
- Comandos de correcao: 2 (resolvectl + netplan)
- Nova chave SSH: ed25519 criada

## Observacoes

- Segundo episodio de DNS no Cohen — primeiro em 28/05, este em 17/06
- Skill `ubuntu-dns-systemd-resolved-fix` documenta o procedimento completo
