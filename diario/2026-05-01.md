# 2026-05-01 — Diário de Bordo

## Patch Max Tokens + Remoção Canva MCP

**S01E08** — Correção do bug de truncamento silencioso no gateway Hermes Agent.

### O que foi feito
- Diagnóstico completo de uso MCP do Cohen (38 sessões, 88 API calls, Composio YouTube ativo)
- Removido Canva MCP (desconectado, causava TaskGroup errors)
- Identificado bug raiz: `gateway/run.py` não passa `max_tokens` para `AIAgent()`
- Dois patches aplicados no `run_agent.py` (init injection + fallback 32768)
- Patches aplicados em ambas as instâncias (DELL + M70q)
- Gateway reiniciado em ambos os hosts sem erros
- Documentado no vault: `Hermes/Tecnologia/Patch_Max_Tokens_Gateway_Bug_2026-05-01.md`

### Métricas Cohen
- Média resposta: 1.018 chars (~255 tokens)
- Pico: 4.250 chars (~1.062 tokens)
- 34% das respostas com 3+ tool calls
- MCP overhead reduzido: 78 → 54 ferramentas (~3.192 tokens/request economizados)

### Lição crítica
- `yaml.dump()` corrompe config.yaml — usar sed ou Python str.replace()
- `patch` tool e `mcp_filesystem_edit_file` destroem indentação .py — usar Python inline com repr()
- Antes de patchar, sempre `git checkout HEAD -- arquivo.py` se algo der errado
