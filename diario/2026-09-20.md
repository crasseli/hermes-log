# Diario de Bordo - 2026-09-20

## Sessoes do Dia

- **Update Hermes 0.21.2 → 0.21.3 e diagnostico do traceback pos-swap:** `hermes update --force` colou o pull de ~5000 commits mas derrubou no passo de limpeza de processos dashboard com `TypeError: _find_stale_dashboard_pids() got an unexpected keyword argument 'scope_home'`. Diagnostico: filho pos-swap importou bytecode velho (`.pyc`) contra fonte nova — assinatura de linhas de traceback desalinhadas. Gateway ficou com codigo 0.21.2 em memoria; restart manual executado e verificado (PID 14076).
- **Health check pos-update:** fleet inteiro confirmado de pe — gateway + Telegram (PID 1124, 0.21.3/`3dae1f73`), Honcho API (`/health` ok, PID 10092), deriver worker (PID 9628, watchdog OK), PostgreSQL 5432, Memurai rodando (fallback in-memory do cache = PIT conhecido), desktop app com 5 processos + backends `serve`.
- **Auditoria dos receipts de update:** reflog revelou 3 updates no dia (06:49 col, 07:11 e 07:21 — o ultimo disparado pelo auto-update do desktop, outcome success). Descoberta: `pre_update_backup` esta com `mode: off` — o script de backup de arquivos sensiveis nao rodou em nenhum dos 3.

## Decisoes e Aprendizados

- Stamps `code_sha`/`code_version` no `gateway_state.json` sao lidos do **checkout em disco**, nao do `sys.modules` do processo — nunca usar o stamp para afirmar que o processo rodando esta na versao nova.
- Crash no passo de limpeza/restart do updater **nao invalida o update**: codigo, deps, skills e config migracao ja tinham colado; restava so o ciclo de restart da frota.
- Auto-update do desktop app existe e roda com `--yes --gateway --force` — explicou o PID novo sem acao do operador.

## Obstaculos e Correcoes

| Problema | Solucao |
|----------|---------|
| Traceback `scope_home` no fim do update | Ignorado como dano (bug upstream de `.pyc` stale); restart manual do gateway |
| Gateway com 0.21.2 em memoria apos update | `hermes gateway restart` — drenou PID 7352, subiu PID 14076 na nova |
| "6 commits atras" logo apos atualizar | Normal — upstream muito ativa; confirmado por `git rev-list` = 0/0 apos updates do desktop |

## Metricas

- Sessoes: 1 (S01E19)
- Artefatos criados: diario, sessao tecnica, metricas 2026-09, nota vault
- Updates aplicados no dia: 3 (d39caf45 → 1ec61e4d → 3dae1f73)

## Observacoes

- Pendencia: decidir `gateway.multiplex_profiles` (migrar p/ multiplex ou fixar false e silenciar aviso).
- Pendencia: reativar/verificar `updates.pre_update_backup` antes do proximo update.
- Proximo passo: reportar bug do limpo dashboard_procs (`.pyc` stale no filho pos-swap) upstream.
