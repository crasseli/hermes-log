# Diario de Bordo - 2026-08-02

## Sessoes do Dia

- **Upgrade Honcho Server 3.0.11 → 3.0.12:** Upgrade completo e validado do servidor de memoria do Hermes (D:\honcho-server). Nao e patch simples — 23 commits substantivos (telemetria Langfuse/CloudEvents, Redis cluster, novo llm/capture.py, document dedup). Backup completo, 7 patches Windows reaplicados, 5 spots de gpt-5.4-mini remapeados, SDK honcho-ai 2.1.2→2.2.0, teste funcional end-to-end aprovado.
- **Decisao de modelos:** Christian optou por **manter `deepseek-chat`** como modelo LLM de todos os pipelines (rapido e barato). Testes de modelos NIM (deepseek-v4-pro, stepfun-ai/step-3.7-flash, llamas, nemotron) concluidos; nenhum substituiu o deepseek-chat.

## Decisoes e Aprendizados

- Manter deepseek-chat para LLM; embedding continua nvidia/nv-embedqa-e5-v5 via NIM (1024 dims validado)
- v3.0.12 NAO corrige problemas Windows: gpt-5.4-mini cresceu de 2 para 5 lugares; uvloop/embedding-lista intactos
- Alembic sem migracao nova — schema do banco NAO muda (risco de corrupcao ≈ zero)
- 4 dos 7 arquivos patchados mudaram upstream — reaplicacao via adaptacao, nao copy-paste
- `git apply` no Windows exige path `D:/...` (nao MSYS `/d/...`)

## Obstaculos e Correcoes

| Problema | Solucao |
|----------|---------|
| SDK honcho-ai 2.2.0 mudou API (peer.session() virou sessions()) | Testar via HTTP /v3 direto (contrato real), nao via ABI do SDK |
| Mensagem /v3 exige messages:[{content, peer_id}] | 4 refinamentos de 422 ate acertar schema |
| Workspace de teste nao deleta com sessao ativa | Apagar sessao primeiro, depois workspace |
| git apply com path MSYS falha | Usar path Windows nativo D:/... |

## Metricas

- Sessoes: 1 (Upgrade Honcho)
- Versao: Honcho v3.0.11 → **3.0.12**
- SDK: honcho-ai 1.21 → **2.2.0**
- Downtime: ~1-2min

## Observacoes

- Proximos passos: limpar VECTOR_STORE_DIMENSIONS do .env (deprecado); remover __pycache__ residuais
- Sessao tecnica completa em S01E18